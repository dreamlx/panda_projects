<tr>
  <td><%= link_to report.id, report %></td>
  <td><%= report.user.name %></td>
  <td><%= report.period.number %></td>
  <td><%= link_to t('edit'), edit_report_path(report) %></td>
  <td><%= link_to t('destroy'), report, 
                  data: {confirm: t('are_you_sure')}, method: :delete %></td>
</tr>