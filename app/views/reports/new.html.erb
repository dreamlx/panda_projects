<%= simple_nested_form_for(@report) do |f|%>
  <%= f.input :user_id, collection: User.select_users %>
  <%= f.input :period_id, collection: Period.order(number: :desc) %>
  <div class="col-md-2">
    <%= f.button :submit, 'refresh', class: "btn btn-danger btn-lg" %>
  </div>
<% end %>