Job code : <%=@deduction.project.job_code%>

<hr>
<% Deduction.content_columns.each do |column| %>
  <p>
    <b><%= column.human_name %>:</b> <%=h @deduction.send(column.name) %>
  </p>
<% end %>
<%= link_to 'Edit', edit_deduction_path(@deduction) if @deduction.project.status.title == 'Active'%> |
<%= link_to 'Back', deductions_url %>
